[package]
name = "domain"
version = "0.1.1"
edition = "2024"

[dependencies]
log = "0.4"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"
ts-rs = { version = "10", optional = true, features = ["serde-compat"] }

[features]
typescript = ["ts-rs"]
